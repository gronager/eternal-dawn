\chapter{Seeing almost Nothing}
\label{chap:almostnothing}

% =====================================================================
% MIGRATION CONTRACT (Stage 4 fills this chapter)  --  visualization track
%   FROM Ch10 Visualization (all): the challenge; the gravity-scaled
%        conformal mapping; visualization targets; implementation; the
%        "art" applications (the two-tier wallpaper, gravity-scaled meaning);
%        computational notes; specific plot ideas; first prototypes.
%   NAMING / FRAMING (per the author): the supraverse is NOT a foam -- the
%        "foam" was the retired packed I<I_crit limit. It is a DILUTE scatter
%        of island universes in an overwhelmingly empty void. The honest
%        picture to render is "almost nothing": the void is the largest chunk
%        of 3D space we have, with tiny pockets of 4D (the universes) inside
%        it. Lead with "supraverse" and "the void"; avoid "foam".
% =====================================================================

\noindent\emph{[Scaffold --- content migrates here in Stage 4.]}
